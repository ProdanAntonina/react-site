console.log(`3.3.2 3`);
let q = 5, w = 2, e = 3;
console.log(`The minimum number is ${Math.min(q, w, e)} and the maximum number is ${Math.max(q, w, e)}`);

//În acest exemplu, folosim metodele Math.min și Math.max pentru a găsi numerele minime și maxime ale lui a, b și, respectiv, c. 
//Rezultatul este apoi înregistrat în consolă.